% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_generation.R
\name{generate_data_mixed}
\alias{generate_data_mixed}
\title{Generate Simulated Mixed-Modality Dataset}
\usage{
generate_data_mixed(
  seed.no,
  param.seed = 4,
  Nd1 = 10,
  Nd2 = 12,
  Nb = 1000,
  Nc = 5,
  Nt = 3,
  Nd1_max = max(Nd1, 100L),
  Nd2_max = max(Nd2, 100L),
  sigma2_b = 0.1,
  theta = c(0.3, 0.15, 0.15, 0.2, 0.2)
)
}
\arguments{
\item{seed.no}{Integer random seed for the subject-level draws: cluster
labels \code{Z}, random effects \code{b}, and the outcomes themselves.}

\item{param.seed}{Integer seed for the true parameters, passed to
\code{\link[=generate_true_params]{generate_true_params()}}. Holding \code{param.seed} fixed while varying
\code{seed.no} gives independent replicates of the same setting.}

\item{Nd1}{Integer number of binary items. Default 10.}

\item{Nd2}{Integer number of continuous items. Default 12.}

\item{Nb}{Integer number of subjects. Default 1000.}

\item{Nc}{Integer number of latent clusters. Default 5.}

\item{Nt}{Integer number of latent constructs. Default 3.}

\item{Nd1_max, Nd2_max}{Item-bank sizes for nested loadings, passed to
\code{\link[=generate_true_params]{generate_true_params()}}.}

\item{sigma2_b}{Variance of the subject random effects (per latent
construct); sets \code{sigma2_b.true <- rep(sigma2_b, Nt)}. Default 0.1.}

\item{theta}{Numeric vector of true cluster weights; must have length \code{Nc}.
Default \code{c(0.3, 0.15, 0.15, 0.2, 0.2)}, the five-cluster simulation setting.}
}
\value{
A named list with:
\itemize{
\item \code{y_1}: Binary outcome matrix (subjects x binary items).
\item \code{y_2}: Continuous outcome matrix (subjects x continuous items).
\item \code{x.true}: True cluster centre matrix (clusters x latent constructs).
\item \code{V.true}: True loading matrix for binary items (constructs x items).
\item \code{U.true}: True loading matrix for continuous items (constructs x items).
\item \code{a_1.true}: True intercepts for binary items.
\item \code{a_2.true}: True intercepts for continuous items.
\item \code{Z.true}: True cluster assignment matrix (clusters x subjects).
\item \code{b.true}: True subject random effects (subjects x latent constructs).
\item \code{theta.true}: True cluster weight vector.
\item \code{sigma2_y_2.true}: True residual variances for continuous items.
\item \code{sigma2_b.true}: True latent variances (per construct).
\item \code{Nc}, \code{Nt}, \code{Nb}: The setting's dimensions, echoed back.
}
}
\description{
Generates a simulated dataset with binary and continuous outcomes based on
the MINDS data generation mechanism, together with the true parameter values
that produced it (so a fit can be scored against the truth).
}
\examples{
\dontrun{
sim_data <- generate_data_mixed(103)
str(sim_data)
}

}
\seealso{
\code{\link[=generate_true_params]{generate_true_params()}}, \code{\link[=MINDS_algorithm]{MINDS_algorithm()}}
}
