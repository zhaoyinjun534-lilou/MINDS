% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MINDS_algorithm.R
\name{MINDS_algorithm}
\alias{MINDS_algorithm}
\title{Fit MINDS Algorithm For Mixed Binary-Continuous Data}
\usage{
MINDS_algorithm(
  y_1,
  y_2,
  Nc,
  Nt,
  iter.max = 800,
  mu_x = 0,
  sigma2_x = 1,
  IG_b.shape = 11,
  IG_b.scale = 1,
  p.theta.prior = rep(1, Nc),
  IG_y_2.shape = 2,
  IG_y_2.scale = 1,
  sigma2_a_1 = 10,
  sigma2_a_2 = 10,
  sigma2_v = 10,
  mu_v = 0,
  sigma2_u = 10,
  mu_u = 0,
  true.values = NULL,
  var_b = 1,
  ref_cluster = 1L,
  init_seed = 123,
  plot_trace = TRUE,
  init_params = NULL,
  update_mu = TRUE,
  update_gamma_U = TRUE,
  update_sigma = TRUE,
  update_b = TRUE
)
}
\arguments{
\item{y_1}{Binary outcome matrix with subjects in rows and items in columns.
Must be a matrix (not a data.frame). \code{NA} entries are allowed: they are
masked out of every conditional update via \code{M_1} and excluded from the
likelihood, i.e. an observed-data (MAR) analysis with no imputation in the
fit itself. Items with no observed variation are dropped from the KAMILA
warm start only.}

\item{y_2}{Continuous outcome matrix with subjects in rows and items in
columns. Same matrix / \code{NA} handling as \code{y_1}, via \code{M_2}.}

\item{Nc}{Integer number of latent clusters.}

\item{Nt}{Integer number of latent constructs.}

\item{iter.max}{Integer number of MCMC iterations.}

\item{mu_x}{Prior mean for cluster centers \code{x}.}

\item{sigma2_x}{Prior variance for cluster centers \code{x}.}

\item{IG_b.shape}{Inverse-gamma shape for random-effect variance \code{sigma2_b}.}

\item{IG_b.scale}{Inverse-gamma scale for random-effect variance \code{sigma2_b}.}

\item{p.theta.prior}{Dirichlet prior parameters for cluster weights.}

\item{IG_y_2.shape}{Inverse-gamma shape for \code{sigma2_y_2}.}

\item{IG_y_2.scale}{Inverse-gamma scale for \code{sigma2_y_2}.}

\item{sigma2_a_1}{Prior variance for binary intercepts.}

\item{sigma2_a_2}{Prior variance for continuous intercepts.}

\item{sigma2_v}{Prior variance for binary loadings.}

\item{mu_v}{Prior mean for binary loadings.}

\item{sigma2_u}{Prior variance for continuous loadings.}

\item{mu_u}{Prior mean for continuous loadings.}

\item{true.values}{Optional list of true parameter values used for initialization and equation solving.
Expected list components are \code{x}, \code{V}, \code{U}, \code{a_1}, \code{a_2}, \code{Z}, and \code{theta}.}

\item{var_b}{Latent variance imposed on each axis by the identifiability
constraint (passed to \code{canonicalize_minds()}). Default \code{1}; set to the
data-generating scale (e.g. \code{0.1}) to report loadings on that metric. Use
the same value for the fit and for any truth-vs-estimate comparison.}

\item{ref_cluster}{Integer in 1:Nc; cluster fixed at the latent-space origin
(location constraint, passed to \code{canonicalize_minds()}). For a truth-vs-
estimate comparison the reported estimates are re-centred so this cluster,
in the truth-aligned labelling, sits at the origin.}

\item{init_seed}{Seed used for initialization and imputation.}

\item{plot_trace}{Logical; if \code{TRUE}, draw the likelihood trace.}

\item{init_params}{Optional list of starting values overriding the defaults.
Recognised components are \code{x}, \code{V}, \code{U}, \code{a_1}, \code{a_2}, \code{sigma2_y_2},
\code{sigma2_b}, \code{b}, \code{Z} and \code{theta}. Supplying \code{Z} skips the \code{mice} + KAMILA
warm start entirely. Combined with the \verb{update_*} flags below this gives a
frozen-parameter decode: parameters whose flag is \code{FALSE} stay fixed at the
values given here for the whole run.}

\item{update_mu}{Logical; if \code{FALSE}, hold the cluster centres \code{x} fixed.}

\item{update_gamma_U}{Logical; if \code{FALSE}, hold the loadings \code{V}, \code{U} and the
intercepts \code{a_1}, \code{a_2} fixed.}

\item{update_sigma}{Logical; if \code{FALSE}, hold \code{sigma2_b} and \code{sigma2_y_2}
fixed.}

\item{update_b}{Logical; if \code{FALSE}, hold the subject random effects \code{b}
fixed.}
}
\value{
A named list with the posterior summaries and model-selection
criteria:
\itemize{
\item \code{membership}: integer vector of estimated cluster labels (length Nb).
\item \verb{cluster center}: Nc x Nt matrix of canonical cluster centres.
\item \verb{loading to binary modality}, \verb{loading to continuous modality}:
Nt x Nd1 and Nt x Nd2 canonical loading matrices.
\item \verb{binary modality intercept}, \verb{continuous modality intercept}: item
intercept vectors.
\item \verb{membership weight}: estimated cluster weights.
\item \code{b}: Nb x Nt subject random effects.
\item \code{sigma2_y_2}, \code{sigma2_b}: residual and latent variances.
\item \verb{axis order}, \verb{axis sign}: modal latent-axis permutation and signs
across the retained draws.
\item \verb{credible intervals}: 95\% intervals for the centres, loadings and
intercepts.
\item \verb{likelihood trace plot}: the per-iteration log-likelihood trace.
\item \code{dic_complete}: the primary model-selection criterion, equal to
\code{dic4_complete}.
\item \code{dic4_complete}, \code{dic5_complete}, \code{dic6_complete} and the matching
\code{pD4_complete}, \code{pD5_complete}, \code{pD6_complete}, \code{Dbar_complete}: the
complete-data DIC family of Celeux et al. (2006). Smaller is better.
DIC4 is the recommended criterion; DIC5 and DIC6 are diagnostics.
}
}
\description{
Runs the Bayesian MINDS model using binary outcomes \code{y_1} and continuous
outcomes \code{y_2}, with user-specified prior hyperparameters.
}
\references{
Celeux, G., Forbes, F., Robert, C. P. and Titterington, D. M. (2006).
Deviance Information Criteria for Missing Data Models.
\emph{Bayesian Analysis}, 1(4), 651-706.
}
\seealso{
\code{\link[=generate_data_mixed]{generate_data_mixed()}}, \code{\link[=canonicalize_minds]{canonicalize_minds()}}
}
