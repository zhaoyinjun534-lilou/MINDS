% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/minds_utils.R
\name{align_axes_to_truth}
\alias{align_axes_to_truth}
\title{Align estimated latent axes to the true axes (simulation evaluation only)}
\usage{
align_axes_to_truth(V.est, U.est, x.est, V.true, U.true)
}
\arguments{
\item{V.est, U.est, x.est}{canonical estimates (V,U: Nt x Nd; x: Nc x Nt).}

\item{V.true, U.true}{canonical true loadings (Nt x Nd).}
}
\value{
list with aligned \code{V}, \code{U}, \code{x}, and the chosen \code{perm} and \code{sign}.
}
\description{
The canonical form orders axes by loading norm and signs them by the largest
loading. When two true axes have similar loading norms, estimation noise can
swap their order (or flip a sign) relative to the truth, so a like-for-like
RMSE needs the estimated axes matched to the true ones. This finds the axis
permutation and per-axis signs minimising the loading distance to the truth
(the axis analogue of resolving cluster label-switching) and applies them to
\code{V}, \code{U} and \code{x}. Intercepts \code{a} are per item, not per axis, so are unchanged.
Uses the true values, so it is for simulation comparison, not real-data fits.
}
