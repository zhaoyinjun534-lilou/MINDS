% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/minds_utils.R
\name{canonicalize_minds}
\alias{canonicalize_minds}
\title{Map MINDS parameters to their identified (canonical) form}
\usage{
canonicalize_minds(x, b, V, U, a_1, a_2, sigma2_b, var_b = 1, ref_cluster = 1L)
}
\arguments{
\item{x}{Nc x Nt matrix of cluster centres.}

\item{b}{Nb x Nt matrix of subject random effects.}

\item{V}{Nt x Nd1 binary loading matrix.}

\item{U}{Nt x Nd2 continuous loading matrix.}

\item{a_1, a_2}{length-Nd1 / length-Nd2 intercept vectors.}

\item{sigma2_b}{length-Nt latent variances (used for the scale step).}

\item{var_b}{target latent variance imposed on each axis by the scale step
(the identifiability constraint on the latent metric). Default \code{1}
(unit-variance constructs); set e.g. \code{0.1} to match the data-generating scale.}

\item{ref_cluster}{integer in 1:Nc; the cluster whose centre is fixed at the
origin (location constraint). Other centres are read as contrasts against it.}
}
\value{
list with canonical \code{x}, \code{b}, \code{V}, \code{U}, \code{a_1}, \code{a_2}, and \code{sigma2_b}
(all equal to \code{var_b} after canonicalisation), plus the \code{order} and \code{sign}.
}
\description{
The MINDS likelihood depends on the parameters only through the invariant
linear predictors \eqn{m_i = (Z_i^T x + b_i) L + a}, where \eqn{L = [V | U]}.
Fixing the latent variance to a common value \code{var_b} makes the latent
covariance \eqn{var_b \cdot I}, which is invariant to any orthogonal rotation;
X, V and U are then identified only up to a rotation (plus per-axis sign and an
overall location shift), even though \eqn{X L + a} -- and hence the intercepts
a -- are well determined. This function removes all of these indeterminacies
deterministically, so no optimisation and no knowledge of the true values is
required. Cluster label-switching is handled separately (e.g. by \code{error.fun}).
}
\details{
Canonical conventions applied, in order:
\enumerate{
\item scale    : each latent axis rescaled so that var(b) = var_b on that axis;
\item rotation : rotate to the loadings' principal axes (eigenbasis of L L^T),
removing the orthogonal-rotation freedom and ordering axes by
descending loading energy;
\item sign     : the largest-magnitude loading on each axis is made positive;
\item location : the reference cluster centre is set to 0.
}

Every step is orthogonal or diagonal in the latent space, so the independence
(diagonal covariance) of \code{b} is preserved.
}
